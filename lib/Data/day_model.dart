class DayModel {
  String title = "";  
  bool isSelected = false;
  bool morning = false;
  bool afternoon = false;
  bool evening = false;

  DayModel(this.title, this.isSelected, this.morning, this.afternoon, this.evening);
}